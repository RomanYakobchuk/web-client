import { useParams} from "react-router-dom";

const ShowFreeSeats = () => {
    const {id} = useParams();

    return (
        <div>
            ShowFreePlaces
        </div>
    );
};
export default ShowFreeSeats
