import {CustomCreate} from "@/components";

const CreateUser = () => {
    return (
        <CustomCreate
            isLoading={false}
            bgColor={'transparent'}
        >
            CreateUser
        </CustomCreate>
    );
};
export default CreateUser
