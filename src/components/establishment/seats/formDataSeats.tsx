import {IFormDataSeats} from "../../../interfaces/formData";

const FormDataSeats = (props: IFormDataSeats) => {
    const {handleSubmit, onFinishHandler, list, setList, establishmentId} = props;
    return (
        <div>
            FormDataSeats
        </div>
    );
};
export default FormDataSeats
