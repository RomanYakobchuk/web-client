
interface IProps {
    id: string,
    type: string
}

const ReviewsList = ({id, type}: IProps) => {
    return (
        <div>
            ReviewsList
        </div>
    );
};
export default ReviewsList

